#include "main.h"

/**
 * _islower - checks if the character is lowercase
 * @c: a charcter check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int_islower(int c)
{

	return (c >= 'a' && c <= 'z');

}
